//SOME ()testa se ao menosun dos elementos do array passam pelo reste implementado na funcaocallback, passando como argumento e retorna true o false.

const letras = ['a', 'b', 'c']

letras.some((letra) =>{
    
    return letra === 'c';
},

);//retorne true

