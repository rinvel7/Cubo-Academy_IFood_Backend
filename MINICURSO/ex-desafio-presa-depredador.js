//PRESA = true
//PREDADOR = false

let olhosNasLaterais = false;


if(olhosNasLaterais === true){
  console.log("PRESA")
}else{
  console.log("PREDADOR")
}

Explicação do código:

Na primeira linha, declaramos uma variável chamada olhosNasLaterais e a inicializamos com o valor false.
Em seguida, utilizamos uma estrutura condicional if...else para verificar se o valor de olhosNasLaterais é igual a true.
Se a condição for verdadeira (ou seja, se olhosNasLaterais for igual a true), o bloco de código dentro do if será executado, e será exibida a mensagem "PRESA".
Caso a condição seja falsa (ou seja, se olhosNasLaterais for igual a false), o bloco de código dentro do else será executado, e será exibida a mensagem "PREDADOR".
Esse código representa uma situação em que estamos verificando se a condição olhosNasLaterais é verdadeira. Se for verdadeira, o código considera que a entidade é uma "presa", e se for falsa, considera que é um "predador". No caso específico do código fornecido, como olhosNasLaterais é false, será exibida a mensagem "PREDADOR".
